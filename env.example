# Chave da API do Google Gemini
# Obtenha em: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_api_key_here

# ============================================
# Configurações para desenvolvimento local
# ============================================
# Use estas variáveis quando rodar a API localmente
# (com outros serviços no Docker)

# Host do Redis (use 'queue' quando a API estiver no Docker, 'localhost' quando local)
REDIS_HOST=localhost
REDIS_PORT=6379

# Host do ChromaDB (use 'db' quando a API estiver no Docker, 'localhost' quando local)
CHROMA_HOST=localhost
CHROMA_PORT=8000

# Porta da API quando rodar localmente
API_PORT=3000

# ============================================
# Configurações do Watcher
# ============================================
# Caminho para o arquivo de scan paths
# Este arquivo contém os caminhos que o watcher deve monitorar
# IMPORTANTE: Use o caminho absoluto, não ~/
# Exemplo: /home/seu_usuario/.config/brain/scan.paths
SCAN_PATHS_FILE=/home/seu_usuario/.config/brain/scan.paths

# Caminho base para montar no container do watcher (usado apenas no Docker)
# Deve ser o caminho que contém os diretórios listados no SCAN_PATHS_FILE
# IMPORTANTE: Configure com o caminho absoluto do seu home
# Exemplo: /home/seu_usuario
SCAN_PATHS_MOUNT=/home/seu_usuario

# Home do usuário (opcional, usado principalmente no Docker)
# Se não definido, será detectado automaticamente pelo watcher
# USER_HOME=/home/seu_usuario

# URL da API para o watcher enviar os arquivos
# Se a API estiver rodando localmente (não no Docker), use:
# API_URL=http://host.docker.internal:3000/queue/ingest
# Se a API estiver rodando no Docker, use:
# API_URL=http://api:3000/queue/ingest
API_URL=http://host.docker.internal:3000/queue/ingest

